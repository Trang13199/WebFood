 <a href="#" class="list-group-item list-group-item-danger active">
            SẢN PHẨM YÊU THÍCH
        </a>
<div class="container marketing col-lg-12">
    <div class="row">
        @foreach (System.Data.DataRow p in ViewBag.sp_yeuthich.Rows)
        {
            <div class="card" style="width: 17rem;">
                <a href="@Url.Action("chi_tiet_sp", "Home", new {ids = @p["product_id"]})">
                    <img src="@p["image"]" class="card-img" alt="..." style="height: 17rem">
                </a>
                <div class="card-body">
                    <h5 class="card-title">@p["name"]</h5>
                    <p class="card-text">@p["price"] VNĐ </p>
                    <div class="cart">
                        <a href="@Url.Action("gio_hang", "Home")"> <button>Mua hàng</button></a>
                        <a href="@Url.Action("ThemVaoGio", "Home", new{SanPhamID = @p["product_id"]})">
                            <span style="font-size: 20px; color: #ff363f; margin-left: 5px">
                                <i class="fas fa-shopping-basket"></i></span></a>
                    </div>
                </div>
            </div>
}
</div>
    </div>